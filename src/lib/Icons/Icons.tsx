import { styled } from '@mui/styles';
import MuiOpenWithIcon from '@mui/icons-material/OpenWith';
import MuiCheckbox from '@mui/icons-material/CheckBox';
import MuiFilterList from '@mui/icons-material/FilterList';

export const OpenWith = styled(MuiOpenWithIcon)({
  color: '#6d6d6d',
});

export const CheckBox = styled(MuiCheckbox)({
  color: '#5a97fd',
});

export const FilterList = styled(MuiFilterList)({
  color: '#375692',
});
